# Runtime dependencies (Python 3.11)
# Keep the DataHub SDK and Agent Context Kit on the same release to avoid
# compatibility problems between their shared metadata and client APIs.
acryl-datahub==1.6.0.6
# Current transport adapter; FlowJury's public environment names remain provider-neutral.
anthropic>=0.49,<1
datahub-agent-context==1.6.0.6
langgraph>=1.0,<2
